*.egg-info
__pycache__
*.zip
ignore*

# ...but keep committed test fixtures so the repo runs from a clean clone.
!testdata/*.zip
dist/
build/

# kicad-cli writes a project-local settings file next to any board it plots.
testdata/*.kicad_prl
